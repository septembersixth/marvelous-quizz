
<div class="col-sm-8 blog-main">
    <?php foreach($posts as $post) : ?>
        <div class="blog-post box-shadow">
            <h2 class="blog-post-title"><span><a href="<?= $this->url('post', ['url' => $post->getUrl()]); ?>"><?= $post->getTitle(); ?></a></span></h2>
            <p class="blog-post-meta"><?= $post->getCreated()->format('M n Y'); ?></p>
            <?php if ($post->getImage()): ?>
            <a href="<?= $this->url('post', ['url' => $post->getUrl()]); ?>"><img class="img-responsive" src="<?= $this->basePath(sprintf('img/upload/%s',$post->getImage())); ?>"/></a>
            <?php endif; ?>
            <br>
            <div class="blog-post-text"><?= substr($post->getText(), 0, 500); ?> ...</div>

            <div class="more-tag-align">
                <a class="more-tag btn btn-default" href="<?= $this->url('post', ['url'=> $post->getUrl()]) ?>"> Read More…</a>
            </div>

            <?= $this->partial('partial/edit.phtml', [ 'post'=>$post ]); ?>
        </div>
    <?php endforeach; ?>

    <?= $this->paginationControl($posts, 'sliding', 'partial/paginator.phtml', ['route' => $this->route,
                                                                                'params' => $this->routeParams
                                                                                ]); ?>
</div>